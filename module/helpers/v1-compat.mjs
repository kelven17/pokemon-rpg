/**
 * v1-compat.mjs (deprecated / stub)
 *
 * Esse módulo existia pra disparar hooks legados pro Theatre Inserts.
 * Foi substituído por um sistema próprio de palco em
 * `module/theatre/stage.mjs`. Mantido como stub vazio porque o sandbox
 * de desenvolvimento não permite remover arquivos — pode ser apagado
 * manualmente do projeto.
 */
export {};
