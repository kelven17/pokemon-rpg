<section class="tab" data-tab="bio" data-group="primary">
    <div class="bio-section">
        <h3>{{localize "POKEMON_RPG.Biography"}}</h3>
        <prose-mirror name="system.biography.value" value="{{system.biography.value}}" toggled></prose-mirror>
    </div>
    <div class="bio-section">
        <h3>{{localize "POKEMON_RPG.Notes"}}</h3>
        <prose-mirror name="system.biography.notes" value="{{system.biography.notes}}" toggled></prose-mirror>
    </div>
</section>
