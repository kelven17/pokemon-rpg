<section class="tab {{#if (eq activeTab 'main')}}active{{/if}}" data-tab="main" data-group="primary">

    {{!-- ===== ATRIBUTOS ===== --}}
    <div class="sheet-section">
        <div class="sheet-section-header">
            <h3 class="sheet-section-title"><i class="fas fa-dumbbell"></i> {{localize "POKEMON_RPG.Attributes"}}</h3>
            <button type="button" class="reset-phases-btn" data-action="phaseResetAll"
                    title="{{localize 'POKEMON_RPG.Phase.ResetAllHint'}}">
                <i class="fas fa-undo"></i> {{localize "POKEMON_RPG.Phase.ResetAll"}}
            </button>
        </div>
        <div class="attributes-grid">
            {{#each attributes as |attr key|}}
            <div class="attribute-card phase-{{attr.phase}}" data-attribute="{{attr.key}}">
                <h3 class="attr-name" data-action="rollAttribute" data-attribute="{{attr.key}}"
                    title="{{localize 'POKEMON_RPG.RollAttributeHint'}}">
                    {{attr.label}}
                </h3>
                <div class="attr-value">
                    <input type="number" name="system.attributes.{{attr.key}}.value" value="{{attr.value}}" data-dtype="Number"/>
                </div>
                {{#if (neq attr.phase 0)}}
                <div class="attr-effective" title="{{localize 'POKEMON_RPG.Phase.EffectiveHint'}}">
                    = {{attr.effective}}
                </div>
                {{/if}}
                <div class="attr-phase-row">
                    <button type="button" class="phase-btn phase-down" data-action="phaseDown"
                            data-attribute="{{attr.key}}" title="{{localize 'POKEMON_RPG.Phase.Down'}}">−</button>
                    <span class="phase-badge" data-action="phaseReset" data-attribute="{{attr.key}}"
                          title="{{localize 'POKEMON_RPG.Phase.ResetHint'}}">{{#if (gt attr.phase 0)}}+{{/if}}{{attr.phase}}</span>
                    <button type="button" class="phase-btn phase-up" data-action="phaseUp"
                            data-attribute="{{attr.key}}" title="{{localize 'POKEMON_RPG.Phase.Up'}}">+</button>
                </div>
                <div class="attr-mod">{{signed attr.mod}}</div>
            </div>
            {{/each}}
        </div>
    </div>

    {{!-- ===== EVASÃO ===== ===== DETALHES ===== (lado a lado) ===== --}}
    <div class="main-two-col">

        <div class="sheet-section">
            <div class="sheet-section-header">
                <h3 class="sheet-section-title"><i class="fas fa-person-running"></i> {{localize "POKEMON_RPG.Evasion.Title"}}</h3>
            </div>
            <div class="evasion-grid">
                <div class="evasion-card">
                    <span class="evasion-label">{{localize "POKEMON_RPG.Evasion.Physical"}}</span>
                    <span class="evasion-value">{{system.evasion.physical}}</span>
                    <span class="evasion-source">{{localize "POKEMON_RPG.Attribute.Def"}}</span>
                </div>
                <div class="evasion-card">
                    <span class="evasion-label">{{localize "POKEMON_RPG.Evasion.Special"}}</span>
                    <span class="evasion-value">{{system.evasion.special}}</span>
                    <span class="evasion-source">{{localize "POKEMON_RPG.Attribute.SpD"}}</span>
                </div>
                <div class="evasion-card">
                    <span class="evasion-label">{{localize "POKEMON_RPG.Evasion.Fast"}}</span>
                    <span class="evasion-value">{{system.evasion.fast}}</span>
                    <span class="evasion-source">{{localize "POKEMON_RPG.Attribute.Spe"}}</span>
                </div>
            </div>
        </div>

        <div class="sheet-section">
            <div class="sheet-section-header">
                <h3 class="sheet-section-title"><i class="fas fa-id-card"></i> {{localize "POKEMON_RPG.TrainerDetails"}}</h3>
            </div>
            <div class="trainer-details">
                <div class="form-group">
                    <label><i class="fas fa-city"></i> {{localize "POKEMON_RPG.Hometown"}}</label>
                    <input type="text" name="system.details.hometown" value="{{system.details.hometown}}"/>
                </div>
                <div class="form-group">
                    <label><i class="fas fa-briefcase"></i> {{localize "POKEMON_RPG.Occupation"}}</label>
                    <input type="text" name="system.details.occupation" value="{{system.details.occupation}}"/>
                </div>
                <div class="form-group">
                    <label><i class="fas fa-cake-candles"></i> {{localize "POKEMON_RPG.Age"}}</label>
                    <input type="number" name="system.details.age" value="{{system.details.age}}" data-dtype="Number"/>
                </div>
                <div class="form-group">
                    <label><i class="fas fa-coins"></i> {{localize "POKEMON_RPG.Money"}}</label>
                    <input type="number" name="system.details.money" value="{{system.details.money}}" data-dtype="Number"/>
                </div>
            </div>
        </div>

    </div>
</section>
